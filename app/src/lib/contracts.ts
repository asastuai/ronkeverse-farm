import type { Address } from "viem";
import FarmCoreAbi from "./abis/FarmCore.json";
import NababaTokenAbi from "./abis/NababaToken.json";

const ZERO: Address = "0x0000000000000000000000000000000000000000";
const env = (key: string): Address => (process.env[key] ?? ZERO) as Address;

export const contracts = {
  ronkeverseNFT: env("NEXT_PUBLIC_RONKEVERSE_NFT"),
  ronkeToken: env("NEXT_PUBLIC_RONKE_TOKEN"),
  nababaToken: env("NEXT_PUBLIC_NABABA_TOKEN"),
  farmCore: env("NEXT_PUBLIC_FARM_CORE"),
} as const;

export const isContractsDeployed =
  contracts.farmCore !== ZERO && contracts.nababaToken !== ZERO;

export const farmCoreAbi = FarmCoreAbi;
export const nababaTokenAbi = NababaTokenAbi;

// ABIs mínimas inline (existentes en Ronin mainnet, no requieren extract)
export const erc20Abi = [
  {
    type: "function",
    name: "balanceOf",
    stateMutability: "view",
    inputs: [{ name: "account", type: "address" }],
    outputs: [{ type: "uint256" }],
  },
  {
    type: "function",
    name: "allowance",
    stateMutability: "view",
    inputs: [
      { name: "owner", type: "address" },
      { name: "spender", type: "address" },
    ],
    outputs: [{ type: "uint256" }],
  },
  {
    type: "function",
    name: "approve",
    stateMutability: "nonpayable",
    inputs: [
      { name: "spender", type: "address" },
      { name: "amount", type: "uint256" },
    ],
    outputs: [{ type: "bool" }],
  },
  {
    type: "function",
    name: "symbol",
    stateMutability: "view",
    inputs: [],
    outputs: [{ type: "string" }],
  },
  {
    type: "function",
    name: "decimals",
    stateMutability: "view",
    inputs: [],
    outputs: [{ type: "uint8" }],
  },
] as const;

export const erc721Abi = [
  {
    type: "function",
    name: "balanceOf",
    stateMutability: "view",
    inputs: [{ name: "owner", type: "address" }],
    outputs: [{ type: "uint256" }],
  },
  {
    type: "function",
    name: "tokenOfOwnerByIndex",
    stateMutability: "view",
    inputs: [
      { name: "owner", type: "address" },
      { name: "index", type: "uint256" },
    ],
    outputs: [{ type: "uint256" }],
  },
  {
    type: "function",
    name: "isApprovedForAll",
    stateMutability: "view",
    inputs: [
      { name: "owner", type: "address" },
      { name: "operator", type: "address" },
    ],
    outputs: [{ type: "bool" }],
  },
  {
    type: "function",
    name: "setApprovalForAll",
    stateMutability: "nonpayable",
    inputs: [
      { name: "operator", type: "address" },
      { name: "approved", type: "bool" },
    ],
    outputs: [],
  },
] as const;
